import { createStore } from 'vuex'

export default createStore({
  state: {
    logInTrue: false,
    loginUser: 'admin',
    passwordUser: 'admin'
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
